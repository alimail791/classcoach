<!doctype html>
<html lang="en">
<head><%- include('partials/head', { title: 'Student login — ClassCoach' }) %></head>
<body>
  <div class="auth-shell">
    <div class="auth-card">
      <h1>Student login</h1>
      <% if (error) { %><div class="flash flash-error"><%= error %></div><% } %>
      <form method="POST" action="/student/login" style="display: flex; flex-direction: column; gap: 16px;">
        <div class="field"><label>Roll number</label><input name="roll_no" required></div>
        <div class="field"><label>PIN</label><input name="pin" required inputmode="numeric"></div>
        <button type="submit" class="btn btn-primary">Log in</button>
      </form>
      <span style="font-size: 13px; color: var(--faint);">Ask your teacher for your roll number and PIN.</span>
      <a href="/login" style="font-size: 13px; color: var(--faint);">Teacher? Log in here →</a>
    </div>
  </div>
</body>
</html>
